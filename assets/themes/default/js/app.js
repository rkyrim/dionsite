$(function () {
	$('.user-control, .vertical').on('click', '#validate-link', function () {
		$('#validate-modal').foundation('open');
		 // var $popup = new Foundation.Reveal($('#validate-modal'));
		 // $popup.open();
	});
});
